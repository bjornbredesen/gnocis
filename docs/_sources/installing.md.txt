Installing
==============

To install via the PyPI package manager, open a terminal and execute

`pip install gnocis`


To build from source, run

`make all`


To install a source build, run

`pip install .`

